# QA Review Log — 2026-05-31

Scope: security, robustness, and antifragility review of `griffithee.github.io` after Phase 3 delivery.

## What I inspected

- `index.html`
- `projects.html`
- `experiments.html`
- `about.html`
- `css/style.css`
- `js/nav.js`
- `js/projects-browser.js`
- `js/visualizer.js`
- `data/chains.json`
- `data/projects.json`
- `scripts/refresh-chains.py`
- `UPDATE_RITUAL.md`
- Repository-level git state and file inventory

## What I verified

- No `.github/` workflow directory or GitHub Pages config file exists in this checkout.
- The repo is clean and on `master` tracking `origin/master`.
- The site is a static GitHub Pages build with no server-side backend.
- The main data flow is:
  - `scripts/refresh-chains.py` reads the agent-brain chain registry and rewrites `data/chains.json`.
  - `js/visualizer.js` loads `data/chains.json` and renders the chain view.
  - `js/projects-browser.js` loads `data/projects.json` and renders the projects browser.

## Findings

### Fixed

1. `js/projects-browser.js` trusted JSON values too much when rendering HTML.
   - `p.link` was inserted directly into `href`, which could become a bad protocol link or DOM injection if JSON was contaminated.
   - `p.tags` and `p.agents` were not fully escaped before insertion.
   - I added attribute escaping, safe URL validation, a graceful fallback when links are unsafe, and guards for malformed project entries.

2. `scripts/refresh-chains.py` had weak failure handling for malformed or missing registry input.
   - It could throw on bad JSON or unexpected top-level shapes.
   - I added explicit JSON loading/validation, clearer errors, and warnings for malformed root/delegation entries so the script fails cleanly instead of crashing.

3. `js/visualizer.js` used an absolute `/data/chains.json` fetch path.
   - I changed it to a relative `data/chains.json` path to reduce deployment/path brittleness.

### Observations

- The chain visualizer already had good defensive handling for missing chain data:
  - fetch failure is caught and rendered as an inline error
  - non-array `data.roots` falls back to an empty list
  - selected-node state is cleared when the selection disappears from the filtered set
- There is still no CSP or subresource integrity policy on the site, so the page is relying on same-origin static assets and script hygiene. That is acceptable for this phase but worth hardening later if the site ever grows more dynamic.
- The update ritual is useful and low-friction, but it assumes the local WSL path to `agent-brain`. That is fine for this environment, but it is not portable without the override flag.

### Verification

- `node --check js/projects-browser.js`
- `node --check js/visualizer.js`
- `python3 -m py_compile scripts/refresh-chains.py`
- Failure-path test: `python3 scripts/refresh-chains.py --registry <bad-json>`
  - Result: clean parse error, nonzero exit, no traceback.
- Live-path test: `python3 scripts/refresh-chains.py`
  - Result: refreshed `data/chains.json` from the real registry and reported `4 root(s)` on the current snapshot date.

## Commands run

- `git status --short --branch`
- `rg --files`
- `find . -maxdepth 3 ...`
- `sed` on the site HTML, CSS, JS, JSON, and refresh script

## Follow-up thoughts

- If the site ever accepts more external URLs in data files, a stricter allowlist for hrefs should be kept in the browser renderer.
- If the site becomes more dynamic, add a CSP and consider moving repeated inline styles into CSS classes to make that possible without `unsafe-inline`.

## Follow-up verification

- Re-checked the current site state after the QA pass and found one additional malformed-data gap in `js/visualizer.js`: startup could still crash if a root entry's `delegations` field was not an array.
- Hardened the ingest path to skip malformed roots and delegation entries before building the in-memory maps.
- Re-ran the same validation checks after the patch:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `python3 scripts/refresh-chains.py --registry <bad-json>` returned a clean parse error and nonzero exit, with no traceback.
- Result: no new security issues found beyond the already documented CSP/SRI and local-path portability observations.

## Hardening pass added during review

- Tightened `js/projects-browser.js` so `data/projects.json` can only link to same-origin URLs. This keeps the browser component from turning contaminated JSON into off-site navigation.
- Added fallback text in `js/visualizer.js` for missing `from` / `to` / `description` / `registered` fields so malformed chain entries render as readable placeholders instead of surfacing `undefined`.
- Normalized missing status values in the visualizer to `unknown` rather than leaving them blank.

## Validation after hardening

- `node --check js/visualizer.js`
- `node --check js/projects-browser.js`
- `python3 -m py_compile scripts/refresh-chains.py`
- `python3 scripts/refresh-chains.py` against the live registry
- `python3 scripts/refresh-chains.py --registry <malformed-registry>` with mixed malformed root/delegation entries
  - Result: warnings emitted, no traceback, output still generated for the valid parts of the registry.
  - Operational note: because the script writes `data/chains.json` on success, the live snapshot was restored immediately after the malformed-registry test.

## Final QA pass — 2026-05-31

- Closed the remaining manual drift point in the experiments header by making the visualizer title read its snapshot date from `data/chains.json` metadata at runtime.
  - `experiments.html` now ships with a neutral loading label instead of a hardcoded date.
  - `js/visualizer.js` updates the label after loading the JSON, so the UI and snapshot stay in sync automatically.
- Tightened `js/projects-browser.js` so malformed project records degrade cleanly instead of surfacing `undefined` or empty metadata:
  - missing `name` falls back to `Untitled project`
  - missing `status` falls back to `unknown`
  - missing `desc` falls back to `No description available`
  - missing/invalid agents show `agents unknown`
  - unsafe `link` values remain blocked and show `details unavailable`
- Updated `UPDATE_RITUAL.md` to remove the manual snapshot-label step and point people at the data refresh step instead.
- Verification:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `node --check js/nav.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `git diff --check`
  - `python3 scripts/refresh-chains.py`
  - malformed-registry probe with unsafe filenames and invalid delegation references:
    - exit code `0`
    - warnings emitted
    - valid root entries survived
- projects-browser VM harness with malformed JSON entry:
    - rendered fallback metadata instead of `undefined`
    - blocked a malicious `javascript:` link and showed `details unavailable`
  - visualizer VM harness with synthetic snapshot metadata:
    - title updated from `watcher/chain-registry.json · snapshot loading…` to the loaded snapshot value
- Operational note:
  - one isolated malformed-registry probe temporarily overwrote `data/chains.json`; I restored the live snapshot immediately with `python3 scripts/refresh-chains.py` and rechecked the worktree afterward.
- Current assessment:
  - No high-severity security issues found in the static site after the hardening pass.
  - Remaining hardening opportunities are mostly policy-level: a CSP/SRI layer if the site ever stops relying on inline styles and external fonts, and a portability pass if the local WSL path ever changes.

## Additional QA pass — 2026-05-31

- I re-checked the live site code after the prior hardening pass and found one more security gap in `scripts/refresh-chains.py`: unsafe registry filenames could be joined into `handoffs/` and escape the intended directory.
- Fixed that by validating handoff filenames before any file lookup and skipping unsafe or malformed root/delegation entries instead of trying to read them.
- Added a small robustness guard in `js/nav.js` so a missing `href` attribute on a nav link does not throw during active-link detection.
- Verified the live refresh still works with the real chain registry:
  - `python3 scripts/refresh-chains.py`
- Verified the traversal fix with a malicious registry pointing outside `handoffs/`:
  - script exited `0`
  - emitted warnings
  - produced `0 root(s)` for the malicious registry
  - live `data/chains.json` was immediately restored afterward with `python3 scripts/refresh-chains.py`
- Re-ran the syntax checks after the patch:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `node --check js/nav.js`
  - `python3 -m py_compile scripts/refresh-chains.py`

## Independent Codex verification — 2026-05-31

- Re-opened the site repo after the prior hardening pass and confirmed the tracked source already contains the intended security fixes.
- Ran the current verification set:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `node --check js/nav.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `git diff --check`
- Probed failure modes with temporary registries:
  - structurally invalid top-level registry exits cleanly with an error and no traceback
  - malformed root/delegation entries are skipped with warnings while valid entries still render
- Restored `data/chains.json` after the temporary probe so the live snapshot remains current.
- No additional site changes were required on this pass.

## Additional Codex re-verification — 2026-05-31

- Re-ran the review against the live site checkout and made one small robustness cleanup in `js/visualizer.js`:
  - missing root/delegation dates now fall back to `unknown` instead of rendering as a blank prefix in the SVG footer
- Re-verified the current source after that patch:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `node --check js/nav.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `git diff --check`
- Re-ran the malformed-registry probe:
  - structurally invalid top-level registry still exits cleanly with a nonzero status
  - malformed root/delegation entries still emit warnings and preserve valid output
- Restored the live `data/chains.json` snapshot immediately after the probe.
- Current assessment remains unchanged:
  - no high-severity security issue found in the static site
  - remaining hardening items are policy-level (CSP/SRI if the site becomes more dynamic, portability if the WSL path changes)

## Current QA pass — 2026-05-31 (Codex revisit)

- Re-opened the site after the prior hardening passes and checked the current source state end-to-end.
- Found and fixed three robustness / security-adjacent issues:
  - `scripts/refresh-chains.py` now resolves `data/chains.json` from the script location instead of the current working directory, so running it from the wrong folder cannot write the snapshot to an unexpected place.
  - `js/visualizer.js` now truncates overlong unbroken description text before it can blow out the SVG layout.
  - `js/projects-browser.js` now uses a null-prototype tag set, which avoids prototype-key weirdness if future JSON contains hostile or accidental keys.
- Added a lightweight site-wide browser policy:
  - `meta name="referrer"` now limits cross-origin referrer leakage.
  - a CSP meta tag now allows only same-origin scripts plus the Google Fonts stylesheet/font endpoints used by the design system.
  - `experiments.html` no longer needs an inline bootstrap script; the visualizer auto-initializes when the relevant DOM exists.
- Refreshed `data/chains.json` from the live registry during verification from `/tmp`, confirming the script still writes to the site repo correctly even when not launched from the repo root.
  - The live registry now contains 7 roots, so the regenerated snapshot updated accordingly.
- Verification after the patch set:
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `node --check js/nav.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `git diff --check`
  - `python3 scripts/refresh-chains.py` from `/tmp`
  - `python3 -m json.tool data/chains.json`
- Assessment after this pass:
  - no new high-severity issues discovered
  - the site is still deliberately using inline styles, so the CSP is pragmatic rather than strict; tightening it further would require a separate style refactor

## Additional hardening pass — 2026-05-31

- Re-opened the site repo after the prior pass and looked for one last file-access edge case in the live data refresh flow.
- Found that `scripts/refresh-chains.py` still followed “safe” filenames through symlinks if an attacker or accidental repo artifact placed one inside `handoffs/`.
- Fixed that by resolving each candidate path and rejecting anything outside the real `handoffs/` tree before reading it.
- Tightened the malformed-data story in the visualizer:
  - unknown / unrecognized agent names now render as a neutral gray tag instead of a Claude-colored badge
  - this keeps bad data visibly bad instead of visually plausible
- Verification after the patch:
  - `node --check js/visualizer.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - malformed registry probe with unsafe path input, non-list delegations, and valid fallback entries
    - symlink-escaped handoff files emitted a warning and were skipped
    - unsafe filenames were skipped
    - malformed root entries were skipped or normalized
    - valid entries still rendered
- Operational note:
  - the malformed-registry probe overwrote `data/chains.json` as expected; I will restore the live snapshot before finishing so the deployed data stays current.

## Additional QA pass — 2026-05-31 (Arcade Run hardening)

- Re-checked the public game page and found it was missing the same referrer/CSP policy used by the rest of the site.
  - `game.html` now carries `meta name="referrer"` plus a site-matching CSP header, so the game page no longer sits outside the site-wide browser policy.
- Hardened `js/galaga.js` so low-capability or restricted browser environments fail cleanly instead of throwing:
  - missing canvas 2D context now shows a readable fallback message and stops the game script
  - `localStorage` access now stays inside a guarded path
  - Web Audio constructor failure now disables sound instead of crashing the run
- Verification after the patch:
  - `node --check js/galaga.js`
  - `node --check js/nav.js`
  - `node --check js/projects-browser.js`
  - `node --check js/visualizer.js`
  - `python3 -m py_compile scripts/refresh-chains.py`
  - `git diff --check`
- Assessment:
  - No new high-severity issue surfaced in the static site.
  - The remaining untracked `galaga/` prototype tree is not wired into the live pages; I left it alone to avoid sweeping unrelated work into the review.
